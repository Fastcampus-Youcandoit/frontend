import styled from "styled-components";
import HomeCarousel from "../components/home/HomeCarousel";

const Home = () => {
  return (
    <div>
      <HomeCarousel />
    </div>
  );
};

export default Home;
